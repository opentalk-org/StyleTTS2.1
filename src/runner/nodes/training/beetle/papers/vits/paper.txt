                                                     Conditional Variational Autoencoder with Adversarial Learning for
                                                                         End-to-End Text-to-Speech


                                                                                 Jaehyeon Kim 1 Jungil Kong 1 Juhee Son 1 2


                                                                    Abstract                             et al., 2016) from the preprocessed text,1 and the second
                                                                                                         stage is to generate raw waveforms conditioned on the in-




arXiv:2106.06103v1 [cs.SD] 11 Jun 2021
                                                 Several recent end-to-end text-to-speech (TTS)          termediate representations (Oord et al., 2016; Kalchbrenner
                                                 models enabling single-stage training and parallel      et al., 2018). Models at each of the two-stage pipelines have
                                                 sampling have been proposed, but their sample           been developed independently.
                                                 quality does not match that of two-stage TTS sys-
                                                                                                         Neural network-based autoregressive TTS systems have
                                                 tems. In this work, we present a parallel end-
                                                                                                         shown the capability of synthesizing realistic speech (Shen
                                                 to-end TTS method that generates more natu-
                                                                                                         et al., 2018; Li et al., 2019), but their sequential generative
                                                 ral sounding audio than current two-stage mod-
                                                                                                         process makes it difficult to fully utilize modern parallel pro-
                                                 els. Our method adopts variational inference aug-
                                                                                                         cessors. To overcome this limitation and improve synthesis
                                                 mented with normalizing flows and an adversarial
                                                                                                         speed, several non-autoregressive methods have been pro-
                                                 training process, which improves the expressive
                                                                                                         posed. In the text-to-spectrogram generation step, extracting
                                                 power of generative modeling. We also propose a
                                                                                                         attention maps from pre-trained autoregressive teacher net-
                                                 stochastic duration predictor to synthesize speech
                                                                                                         works (Ren et al., 2019; Peng et al., 2020) is attempted to
                                                 with diverse rhythms from input text. With the
                                                                                                         decrease the difficulty of learning alignments between text
                                                 uncertainty modeling over latent variables and
                                                                                                         and spectrograms. More recently, likelihood-based methods
                                                 the stochastic duration predictor, our method ex-
                                                                                                         further eliminate the dependency on external aligners by
                                                 presses the natural one-to-many relationship in
                                                                                                         estimating or learning alignments that maximize the likeli-
                                                 which a text input can be spoken in multiple ways
                                                                                                         hood of target mel-spectrograms (Zeng et al., 2020; Miao
                                                 with different pitches and rhythms. A subjective
                                                                                                         et al., 2020; Kim et al., 2020). Meanwhile, generative adver-
                                                 human evaluation (mean opinion score, or MOS)
                                                                                                         sarial networks (GANs) (Goodfellow et al., 2014) have been
                                                 on the LJ Speech, a single speaker dataset, shows
                                                                                                         explored in second stage models. GAN-based feed-forward
                                                 that our method outperforms the best publicly
                                                                                                         networks with multiple discriminators, each distinguishing
                                                 available TTS systems and achieves a MOS com-
                                                                                                         samples at different scales or periods, achieve high-quality
                                                 parable to ground truth.
                                                                                                         raw waveform synthesis (Kumar et al., 2019; Bińkowski
                                                                                                         et al., 2019; Kong et al., 2020).
                                                                                                         Despite the progress of parallel TTS systems, two-stage
                                         1. Introduction                                                 pipelines remain problematic because they require sequen-
                                         Text-to-speech (TTS) systems synthesize raw speech wave-        tial training or fine-tuning (Shen et al., 2018; Weiss et al.,
                                         forms from given text through several components. With          2020) for high-quality production wherein latter stage mod-
                                         the rapid development of deep neural networks, TTS sys-         els are trained with the generated samples of earlier stage
                                         tem pipelines have been simplified to two-stage genera-         models. In addition, their dependency on predefined inter-
                                         tive modeling apart from text preprocessing such as text        mediate features precludes applying learned hidden repre-
                                         normalization and phonemization. The first stage is to          sentations to obtain further improvements in performance.
                                         produce intermediate speech representations such as mel-        Recently, several works, i.e., FastSpeech 2s (Ren et al.,
                                         spectrograms (Shen et al., 2018) or linguistic features (Oord   2021) and EATS (Donahue et al., 2021), have proposed
                                                                                                         efficient end-to-end training methods such as training over
                                             1
                                              Kakao Enterprise, Seongnam-si, Gyeonggi-do, Repub-         short audio clips rather than entire waveforms, leveraging a
                                         lic of Korea 2 School of Computing, KAIST, Daejeon, Re-         mel-spectrogram decoder to aid text representation learning,
                                         public of Korea.      Correspondence to: Jaehyeon Kim
                                         <jay.xyz@kakaoenterprise.com>.                                      1
                                                                                                               Although there is a text preprocessing step in TTS systems,
                                                                                                         We herein use preprocessed text interchangeably with the word
                                         Proceedings of the 38 th International Conference on Machine    “text”.
                                         Learning, PMLR 139, 2021. Copyright 2021 by the author(s).
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

and designing a specialized spectrogram loss to relax length-
                                                                                             h                 qφ (z|x) i
                                                                     log pθ (x|c) ≥ Eqφ (z|x) log pθ (x|z)−log               (1)
mismatch between target and generated speech. However,                                                         pθ (z|c)
despite potentially improving performance by utilizing the        where pθ (z|c) denotes a prior distribution of the latent vari-
learned representations, their synthesis quality lags behind      ables z given condition c, pθ (x|z) is the likelihood func-
two-stage systems.                                                tion of a data point x, and qφ (z|x) is an approximate pos-
In this work, we present a parallel end-to-end TTS method         terior distribution. The training loss is then the negative
that generates more natural sounding audio than cur-              ELBO, which can be viewed as the sum of reconstruc-
rent two-stage models. Using a variational autoencoder            tion loss − log pθ (x|z) and KL divergence log qφ (z|x) −
(VAE) (Kingma & Welling, 2014), we connect two modules            log pθ (z|c), where z ∼ qφ (z|x).
of TTS systems through latent variables to enable efficient
end-to-end learning. To improve the expressive power of           2.1.2. R ECONSTRUCTION LOSS
our method so that high-quality speech waveforms can be           As a target data point in the reconstruction loss, we use a
synthesized, we apply normalizing flows to our conditional        mel-spectrogram instead of a raw waveform, denoted by
prior distribution and adversarial training on the waveform       xmel . We upsample the latent variables z to the waveform
domain. In addition to generating fine-grained audio, it is       domain ŷ through a decoder and transform ŷ to the mel-
important for TTS systems to express the one-to-many rela-        spectrogram domain x̂mel . Then the L1 loss between the
tionship in which text input can be spoken in multiple ways       predicted and target mel-spectrogram is used as the recon-
with different variations (e.g., pitch and duration). To tackle   struction loss:
the one-to-many problem, we also propose a stochastic du-
ration predictor to synthesize speech with diverse rhythms                         Lrecon = kxmel − x̂mel k1                 (2)
from input text. With the uncertainty modeling over latent        This can be viewed as maximum likelihood estimation as-
variables and the stochastic duration predictor, our method       suming a Laplace distribution for the data distribution and
captures speech variations that cannot be represented by          ignoring constant terms. We define the reconstruction loss in
text.                                                             the mel-spectrogram domain to improve the perceptual qual-
Our method obtains more natural sounding speech and               ity by using a mel-scale that approximates the response of
higher sampling efficiency than the best publicly avail-          the human auditory system. Note that the mel-spectrogram
able TTS system, Glow-TTS (Kim et al., 2020) with HiFi-           estimation from a raw waveform does not require trainable
GAN (Kong et al., 2020). We make both our demo page               parameters as it only uses STFT and linear projection onto
and source-code publicly available.2                              the mel-scale. Furthermore, the estimation is only employed
                                                                  during training, not inference. In practice, we do not upsam-
                                                                  ple the whole latent variables z but use partial sequences as
2. Method                                                         an input for the decoder, which is the windowed generator
In this section, we explain our proposed method and the ar-       training used for efficient end-to-end training (Ren et al.,
chitecture of it. The proposed method is mostly described in      2021; Donahue et al., 2021).
the first three subsections: a conditional VAE formulation;
alignment estimation derived from variational inference;          2.1.3. KL- DIVERGENCE
adversarial training for improving synthesis quality. The         The input condition of the prior encoder c is composed of
overall architecture is described at the end of this section.     phonemes ctext extracted from text and an alignment A be-
Figures 1a and 1b show the training and inference proce-          tween phonemes and latent variables. The alignment is a
dures of our method, respectively. From now on, we will           hard monotonic attention matrix with |ctext | × |z| dimen-
refer to our method as Variational Inference with adversarial     sions representing how long each input phoneme expands to
learning for end-to-end Text-to-Speech (VITS).                    be time-aligned with the target speech. Because there are no
                                                                  ground truth labels for the alignment, we must estimate the
2.1. Variational Inference                                        alignment at each training iteration, which we will discuss
2.1.1. OVERVIEW                                                   in Section 2.2.1. In our problem setting, we aim to provide
                                                                  more high-resolution information for the posterior encoder.
VITS can be expressed as a conditional VAE with the ob-           We, therefore, use the linear-scale spectrogram of target
jective of maximizing the variational lower bound, also           speech xlin as input rather than the mel-spectrogram. Note
called the evidence lower bound (ELBO), of the intractable        that the modified input does not violate the properties of
marginal log-likelihood of data log pθ (x|c):                     variational inference. The KL divergence is then:

   2
                                                                           Lkl = log qφ (z|xlin ) − log pθ (z|ctext , A),    (3)
   Source-code: https://github.com/jaywalnut310/vits
Demo: https://jaywalnut310.github.io/vits-demo/index.html                z ∼ qφ (z|xlin ) = N (z; µφ (xlin ), σφ (xlin ))
                 Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech




                               (a) Training procedure                                                    (b) Inference procedure

Figure 1. System diagram depicting (a) training procedure and (b) inference procedure. The proposed model can be viewed as a conditional
VAE; a posterior encoder, decoder, and conditional prior (green blocks: a normalizing flow, linear projection layer, and text encoder) with
a flow-based stochastic duration predictor.


The factorized normal distribution is used to parameterize              text in order without skipping any words. To find the
our prior and posterior encoders. We found that increasing              optimum alignment, Kim et al. (2020) use dynamic pro-
the expressiveness of the prior distribution is important for           gramming. Applying MAS directly in our setting is dif-
generating realistic samples. We, therefore, apply a normal-            ficult because our objective is the ELBO, not the exact
izing flow fθ (Rezende & Mohamed, 2015), which allows                   log-likelihood. We, therefore, redefine MAS to find an
an invertible transformation of a simple distribution into a            alignment that maximizes the ELBO, which reduces to find-
more complex distribution following the rule of change-of-              ing an alignment that maximizes the log-likelihood of the
variables, on top of the factorized normal prior distribution:          latent variables z:
                                                 ∂fθ (z)                                                          qφ (z|xlin )
    pθ (z|c) = N (fθ (z); µθ (c), σθ (c)) det            ,     (4)      arg max log pθ (xmel |z) − log
                                                   ∂z                       Â                                  pθ (z|ctext , Â)
           c = [ctext , A]                                              = arg max log pθ (z|ctext , Â)
                                                                                 Â

2.2. Alignment Estimation                                                             = log N (fθ (z); µθ (ctext , Â), σθ (ctext , Â))   (6)
2.2.1. M ONOTONIC ALIGNMENT SEARCH                                      Due to the resemblance of Equation 5 to Equation 6, we can
                                                                        use the original MAS implementation without modification.
To estimate an alignment A between input text and
                                                                        Appendix A includes pseudocode for MAS.
target speech, we adopt Monotonic Alignment Search
(MAS) (Kim et al., 2020), a method to search an align-
                                                                        2.2.2. D URATION PREDICTION FROM TEXT
ment that maximizes the likelihood of data parameterized
by a normalizing flow f :                                               We can calculate the duration of each input token di by sum-
                                                                        ming
                                                                        P all the columns in each row of the estimated alignment
  A = arg max log p(x|ctext , Â)
            Â                                                             j Ai,j . The duration could be used to train a determinis-
                                                                        tic duration predictor, as proposed in previous work (Kim
     = arg max log N (f (x); µ(ctext , Â), σ(ctext , Â)) (5)          et al., 2020), but it cannot express the way a person utters at
            Â
                                                                        different speaking rates each time. To generate human-like
where the candidate alignments are restricted to be mono-               rhythms of speech, we design a stochastic duration predictor
tonic and non-skipping following the fact that humans read              so that its samples follow the duration distribution of given
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

phonemes. The stochastic duration predictor is a flow-based            the discriminator with Nl number of features. Notably, the
generative model that is typically trained via maximum like-           feature matching loss can be seen as reconstruction loss that
lihood estimation. The direct application of maximum likeli-           is measured in the hidden layers of the discriminator sug-
hood estimation, however, is difficult because the duration of         gested as an alternative to the element-wise reconstruction
each input phoneme is 1) a discrete integer, which needs to            loss of VAEs (Larsen et al., 2016).
be dequantized for using continuous normalizing flows, and
2) a scalar, which prevents high-dimensional transformation            2.4. Final Loss
due to invertibility. We apply variational dequantization (Ho
et al., 2019) and variational data augmentation (Chen et al.,          With the combination of VAE and GAN training, the total
2020) to solve these problems. To be specific, we introduce            loss for training our conditional VAE can be expressed as
two random variables u and ν, which have the same time                 follows:
resolution and dimension as that of the duration sequence d,            Lvae = Lrecon + Lkl + Ldur + Ladv (G) + Lf m (G) (11)
for variational dequatization and variational data augmenta-
tion, respectively. We restrict the support of u to be [0, 1) so       2.5. Model Architecture
that the difference d − u becomes a sequence of positive real
numbers, and we concatenate ν and d channel-wise to make               The overall architecture of the proposed model consists of
a higher dimensional latent representation. We sample the              a posterior encoder, prior encoder, decoder, discriminator,
two variables through an approximate posterior distribution            and stochastic duration predictor. The posterior encoder and
qφ (u, ν|d, ctext ). The resulting objective is a variational          discriminator are only used for training, not for inference.
lower bound of the log-likelihood of the phoneme duration:             Architectural details are available in Appendix B.

   log pθ (d|ctext ) ≥                                                 2.5.1. P OSTERIOR E NCODER
                                h    pθ (d − u, ν|ctext ) i            For the posterior encoder, we use the non-causal WaveNet
              Eqφ (u,ν|d,ctext ) log                             (7)   residual blocks used in WaveGlow (Prenger et al., 2019)
                                      qφ (u, ν|d, ctext )
                                                                       and Glow-TTS (Kim et al., 2020). A WaveNet residual
The training loss Ldur is then the negative variational lower          block consists of layers of dilated convolutions with a gated
bound. We apply the stop gradient operator (van den Oord               activation unit and skip connection. The linear projection
et al., 2017), which prevents back-propagating the gradient            layer above the blocks produces the mean and variance of
of inputs, to the input conditions so that the training of the         the normal posterior distribution. For the multi-speaker case,
duration predictor does not affect that of other modules.              we use global conditioning (Oord et al., 2016) in residual
                                                                       blocks to add speaker embedding.
The sampling procedure is relatively simple; the phoneme
duration is sampled from random noise through the inverse
                                                                       2.5.2. P RIOR E NCODER
transformation of the stochastic duration predictor, and then
it is converted to integers.                                           The prior encoder consists of a text encoder that processes
                                                                       the input phonemes ctext and a normalizing flow fθ that
2.3. Adversarial Training                                              improves the flexibility of the prior distribution. The text
                                                                       encoder is a transformer encoder (Vaswani et al., 2017) that
To adopt adversarial training in our learning system, we add           uses relative positional representation (Shaw et al., 2018)
a discriminator D that distinguishes between the output gen-           instead of absolute positional encoding. We can obtain the
erated by the decoder G and the ground truth waveform y.               hidden representation htext from ctext through the text en-
In this work, we use two types of loss successfully applied in         coder and a linear projection layer above the text encoder
speech synthesis; the least-squares loss function (Mao et al.,         that produces the mean and variance used for constructing
2017) for adversarial training, and the additional feature-            the prior distribution. The normalizing flow is a stack of
matching loss (Larsen et al., 2016) for training the generator:        affine coupling layers (Dinh et al., 2017) consisting of a
                     h                               i                 stack of WaveNet residual blocks. For simplicity, we design
  Ladv (D) = E(y,z) (D(y) − 1)2 + (D(G(z)))2 ,             (8)         the normalizing flow to be a volume-preserving transforma-
                  h                  i                                 tion with the Jacobian determinant of one. For the multi-
  Ladv (G) = Ez (D(G(z)) − 1)2 ,                           (9)
                                                                       speaker setting, we add speaker embedding to the residual
                       T
                      hX 1                                  i          blocks in the normalizing flow through global conditioning.
  Lf m (G) = E(y,z)                 kDl (y) − Dl (G(z))k1       (10)
                               Nl
                         l=1                                           2.5.3. D ECODER
where T denotes the total number of layers in the discrim-             The decoder is essentially the HiFi-GAN V1 genera-
inator and Dl outputs the feature map of the l-th layer of             tor (Kong et al., 2020). It is composed of a stack of trans-
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

posed convolutions, each of which is followed by a multi-         3.2. Preprocessing
receptive field fusion module (MRF). The output of the
                                                                  We use linear spectrograms which can be obtained from
MRF is the sum of the output of residual blocks that have
                                                                  raw waveforms through the Short-time Fourier transform
different receptive field sizes. For the multi-speaker setting,
                                                                  (STFT), as input of the posterior encoder. The FFT size,
we add a linear layer that transforms speaker embedding
                                                                  window size and hop size of the transform are set to 1024,
and add it to the input latent variables z.
                                                                  1024 and 256, respectively. We use 80 bands mel-scale
                                                                  spectrograms for reconstruction loss, which is obtained by
2.5.4. D ISCRIMINATOR
                                                                  applying a mel-filterbank to linear spectrograms.
We follow the discriminator architecture of the multi-period
                                                                  We use International Phonetic Alphabet (IPA) sequences as
discriminator proposed in HiFi-GAN (Kong et al., 2020).
                                                                  input to the prior encoder. We convert text sequences to IPA
The multi-period discriminator is a mixture of Markovian
                                                                  phoneme sequences using open-source software (Bernard,
window-based sub-discriminators (Kumar et al., 2019), each
                                                                  2021), and the converted sequences are interspersed with a
of which operates on different periodic patterns of input
                                                                  blank token following the implementation of Glow-TTS.
waveforms.

2.5.5. S TOCHASTIC D URATION P REDICTOR                           3.3. Training

The stochastic duration predictor estimates the distribu-         The networks are trained using the AdamW opti-
tion of phoneme duration from a conditional input htext .         mizer (Loshchilov & Hutter, 2019) with β1 = 0.8, β2 =
For the efficient parameterization of the stochastic dura-        0.99 and weight decay λ = 0.01. The learning rate decay is
tion predictor, we stack residual blocks with dilated and         scheduled by a 0.9991/8 factor in every epoch with an initial
depth-separable convolutional layers. We also apply neural        learning rate of 2 × 10−4 . Following previous work (Ren
spline flows (Durkan et al., 2019), which take the form of        et al., 2021; Donahue et al., 2021), we adopt the windowed
invertible nonlinear transformations by using monotonic           generator training, a method of generating only a part of
rational-quadratic splines, to coupling layers. Neural spline     raw waveforms to reduce the training time and memory
flows improve transformation expressiveness with a similar        usage during training. We randomly extract segments of
number of parameters compared to commonly used affine             latent representations with a window size of 32 to feed to
coupling layers. For the multi-speaker setting, we add a          the decoder instead of feeding entire latent representations
linear layer that transforms speaker embedding and add it to      and also extract the corresponding audio segments from the
the input htext .                                                 ground truth raw waveforms as training targets. We use
                                                                  mixed precision training on 4 NVIDIA V100 GPUs. The
                                                                  batch size is set to 64 per GPU and the model is trained up
3. Experiments                                                    to 800k steps.
3.1. Datasets
                                                                  3.4. Experimental Setup for Comparison
We conducted experiments on two different datasets. We
used the LJ Speech dataset (Ito, 2017) for compari-               We compared our model with the best publicly available
son with other publicly available models and the VCTK             models. We used Tacotron 2, an autoregressive model, and
dataset (Veaux et al., 2017) to verify whether our model          Glow-TTS, a flow-based non-autoregressive model, as first
can learn and express diverse speech characteristics. The         stage models and HiFi-GAN as a second stage model. We
LJ Speech dataset consists of 13,100 short audio clips of a       used their public implementations and pre-trained weights.3
single speaker with a total length of approximately 24 hours.     Since a two-stage TTS system can theoretically achieve
The audio format is 16-bit PCM with a sample rate of 22           higher synthesis quality through sequential training, we in-
kHz, and we used it without any manipulation. We ran-             cluded the fine-tuned HiFi-GAN up to 100k steps with the
domly split the dataset into a training set (12,500 samples),     predicted outputs from the first stage models. We empiri-
validation set (100 samples), and test set (500 samples). The     cally found that fine-tuning HiFi-GAN with the generated
VCTK dataset consists of approximately 44,000 short audio         mel-spectrograms from Tacotron 2 under teacher-forcing
clips uttered by 109 native English speakers with various         mode, led to better quality for both Tacotron 2 and Glow-
accents. The total length of the audio clips is approximately     TTS than fine-tuning with the generated mel-spectrograms
44 hours. The audio format is 16-bit PCM with a sample            from Glow-TTS, so we appended the better fine-tuned HiFi-
rate of 44 kHz. We reduced the sample rate to 22 kHz.                3
                                                                      The implementations are as follows:
We randomly split the dataset into a training set (43,470         Tacotron 2 : https://github.com/NVIDIA/tacotron2
samples), validation set (100 samples), and test set (500         Glow-TTS : https://github.com/jaywalnut310/glow-tts
samples).                                                         HiFi-GAN : https://github.com/jik876/hifi-gan
               Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

GAN to both Tacotron 2 and Glow-TTS.                            the normalizing flow in the prior encoder results in a 1.52
                                                                MOS decrease from the baseline, demonstrating that the
As each model has a degree of randomness during sampling,
                                                                prior distribution’s flexibility significantly influences the
we fixed hyper-parameters that controls the randomness of
                                                                synthesis quality. Replacing the linear-scale spectrogram
each model throughout our experiments. The probability of
                                                                for posterior input with the mel-spectrogram results in a
dropout in the pre-net of Tactron 2 was set to 0.5. For Glow-
                                                                quality degradation (-0.19 MOS), indicating that the high-
TTS, the standard deviation of the prior distribution was set
                                                                resolution information is effective for VITS in improving
to 0.333. For VITS, the standard deviation of input noise
                                                                the synthesis quality.
of the stochastic duration predictor was set to 0.8 and we
multiplied a scale factor of 0.667 to the standard deviation
of the prior distribution.                                            Table 2. MOS comparison in the ablation studies.


4. Results                                                              Model                           MOS (CI)

4.1. Speech Synthesis Quality                                           Ground Truth                   4.50 (±0.06)
                                                                        Baseline                       4.50 (±0.06)
We conducted crowd-sourced MOS tests to evaluate the                    without Normalizing Flow       2.98 (±0.08)
quality. Raters listened to randomly selected audio samples,            with Mel-spectrogram           4.31 (±0.08)
and rated their naturalness on a 5 point scale from 1 to 5.
Raters were allowed to evaluate each audio sample once,
and we normalized all the audio clips to avoid the effect       4.2. Generalization to Multi-Speaker Text-to-Speech
of amplitude differences on the score. All of the quality
                                                                To verify that our model can learn and express diverse
assessments in this work were conducted in this manner.
                                                                speech characteristics, we compared our model to Tacotron
The evaluation results are shown in Table 1. VITS outper-       2, Glow-TTS and HiFi-GAN, which showed the ability to
forms other TTS systems and achieves a similar MOS to that      extend to multi-speaker speech synthesis (Jia et al., 2018;
of ground truth. The VITS (DDP), which employs the same         Kim et al., 2020; Kong et al., 2020). We trained the mod-
deterministic duration predictor architecture used in Glow-     els on the VCTK dataset. We added speaker embedding
TTS rather than the stochastic duration predictor, scores the   to our model as described in Section 2.5. For Tacotron
second-highest among TTS systems in the MOS evaluation.         2, we broadcasted speaker embedding and concatenated it
These results imply that 1) the stochastic duration predictor   with the encoder output, and for Glow-TTS, we applied the
generates more realistic phoneme duration than the deter-       global conditioning following the previous work. The eval-
ministic duration predictor and 2) our end-to-end training      uation method is the same as that described in Section 4.1.
method is an effective way to make better samples than          As shown in Table 3, our model achieves a higher MOS
other TTS models even if maintaining the similar duration       than the other models. This demonstrates that our model
predictor architecture.                                         learns and expresses various speech characteristics in an
                                                                end-to-end manner.

Table 1. Comparison of evaluated MOS with 95% confidence in-
tervals on the LJ Speech dataset.                               Table 3. Comparison of evaluated MOS with 95% confidence in-
                                                                tervals on the VCTK dataset.

  Model                                      MOS (CI)             Model                                       MOS (CI)
  Ground Truth                              4.46 (±0.06)          Ground Truth                               4.38 (±0.07)
  Tacotron 2 + HiFi-GAN                     3.77 (±0.08)          Tacotron 2 + HiFi-GAN                      3.14 (±0.09)
  Tacotron 2 + HiFi-GAN (Fine-tuned)        4.25 (±0.07)          Tacotron 2 + HiFi-GAN (Fine-tuned)         3.19 (±0.09)
  Glow-TTS + HiFi-GAN                       4.14 (±0.07)          Glow-TTS + HiFi-GAN                        3.76 (±0.07)
  Glow-TTS + HiFi-GAN (Fine-tuned)          4.32 (±0.07)          Glow-TTS + HiFi-GAN (Fine-tuned)           3.82 (±0.07)
  VITS (DDP)                                4.39 (±0.06)          VITS                                       4.38 (±0.06)
  VITS                                      4.43 (±0.06)

We conducted an ablation study to demonstrate the effec-
                                                                4.3. Speech Variation
tiveness of our methods, including the normalized flow in
the prior encoder and linear-scale spectrogram posterior in-    We verified how many different lengths of speech the
put. All models in the ablation study were trained up to        stochastic duration predictor produces, and how many dif-
300k steps. The results are shown in Table 2. Removing          ferent speech characteristics the synthesized samples have.
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech




                                                                                                (a) VITS



(a) Comparison of sample duration. Glow-TTS only provides a
single value due to the deterministic duration predictor.


                                                                                              (b) Tacotron 2




                                                                                              (c) Glow-TTS

    (b) Comparison of sample duration in different speakers.

Figure 2. Sample duration in seconds on (a) the LJ Speech dataset
and (b) the VCTK dataset.

                                                                                        (d) VITS (multi-speaker)
Similar to Valle et al. (2021), all samples here were gen-
erated from a sentence “How much variation is there?”.              Figure 3. Pitch tracks for the utterance “How much variation is
Figure 2a shows histograms of the lengths of 100 generated          there?”. Samples are generated from (a) VITS, (b) Tacotron 2, and
utterances from each model. While Glow-TTS generates                (c) Glow-TTS in the single speaker setting and from (d) VITS in
only fixed-length utterances due to the deterministic du-           the multi-speaker setting.
ration predictor, samples from our model follow a similar
length distribution to that of Tacotron 2. Figure 2b shows the
lengths of 100 utterances generated with each of five speaker
identities from our model in the multi-speaker setting, im-         with 100 sentences randomly selected from the test set of the
plying that the model learns the speaker-dependent phoneme          LJ Speech dataset. We used a single NVIDIA V100 GPU
duration. F0 contours of 10 utterances extracted with the           with a batch size of 1. The results are shown in Table 4.
YIN algorithm (De Cheveigné & Kawahara, 2002) in Fig-              Since our model does not require modules for generating
ure 3 shows that our model generates speech with diverse            predefined intermediate representations, its sampling effi-
pitches and rhythms, and five samples generated with each           ciency and speed are greatly improved.
of different speaker identities in Figure 3d demonstrates
our model expresses very different lengths and pitches of
                                                                    5. Related Work
speech for each speaker identity. Note that Glow-TTS could
increase the diversity of pitch by increasing the standard          5.1. End-to-End Text-to-Speech
deviation of the prior distribution, but on the contrary, it
could lower the synthesis quality.                                  Currently, neural TTS models with a two-stage pipeline can
                                                                    synthesize human-like speech (Oord et al., 2016; Ping et al.,
                                                                    2018; Shen et al., 2018). However, they typically require
4.4. Synthesis Speed
                                                                    vocoders trained or fine-tuned with first stage model output,
We compared the synthesis speed of our model with a paral-          which causes training and deployment inefficiency. They
lel two-stage TTS system, Glow-TTS and HiFi-GAN. We                 are also unable to reap the potential benefits of an end-to-
measured the synchronized elapsed time over the entire pro-         end approach that can use learned hidden representations
cess to generate raw waveforms from phoneme sequences               rather than predefined intermediate features.
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

                                                                   Rezende & Mohamed (2015), Chen et al. (2017) and Ziegler
Table 4. Comparison of the synthesis speed. Speed of n kHz means
                                                                   & Rush (2019) improve VAE performance by enhancing
that the model can generate n×1000 raw audio samples per second.
Real-time means the synthesis speed over real-time.                the expressive power of prior and posterior distribution with
                                                                   normalizing flows. To improve the representation power
                                                                   of the prior distribution, we add normalizing flows to our
   Model                        Speed (kHz)      Real-time
                                                                   conditional prior network, leading to the generation of more
   Glow-TTS + HiFi-GAN              606.05        ×27.48           realistic samples.
   VITS                            1480.15        ×67.12
   VITS (DDP)                      2005.03        ×90.93           Similar to our work, Ma et al. (2019) proposed a condi-
                                                                   tional VAE with normalizing flows in a conditional prior
                                                                   network for non-autoregressive neural machine translation,
                                                                   FlowSeq. However, the fact that our model can explicitly
                                                                   align a latent sequence with the source sequence differs from
Recently, single-stage end-to-end TTS models have been             FlowSeq, which needs to learn implicit alignment through
proposed to tackle the more challenging task of generat-           attention mechanisms. Our model removes the burden of
ing raw waveforms, which contain richer information (e.g.,         transforming the latent sequence into standard normal ran-
high-frequency response and phase) than mel-spectrograms,          dom variables by matching the latent sequence with the
directly from text. FastSpeech 2s (Ren et al., 2021) is an         time-aligned source sequence via MAS, which allows for
extension of FastSpeech 2 that enables end-to-end parallel         simpler architecture of normalizing flows.
generation by adopting adversarial training and an auxil-
iary mel-spectrogram decoder that helps learn text repre-          5.3. Duration Prediction in Non-Autoregressive
sentations. However, to resolve the one-to-many problem,                Text-to-Speech
FastSpeech 2s must extract phoneme duration, pitch, and
energy from speech used as input conditions in training.           Autoregressive TTS models (Taigman et al., 2018; Shen
EATS (Donahue et al., 2021) employs adversarial training           et al., 2018; Valle et al., 2021) generate diverse speech with
as well and a differentiable alignment scheme. To resolve          different rhythms through their autoregressive structure and
the length mismatch problem between generated and target           several tricks including maintaining dropout probability dur-
speech, EATS adopts soft dynamic time warping loss that is         ing inference and priming (Graves, 2013). Parallel TTS
calculated by dynamic programming. Wave Tacotron (Weiss            models (Ren et al., 2019; Peng et al., 2020; Kim et al.,
et al., 2020) combines normalizing flows with Tacotron 2           2020; Ren et al., 2021; Lee et al., 2021), on the other hand,
for an end-to-end structure but remains autoregressive. The        have been relied on deterministic duration prediction. It is
audio quality of all the aforementioned end-to-end TTS             because parallel models have to predict target phoneme dura-
models is less than that of two-stage models.                      tion or the total length of target speech in one feed-forward
                                                                   path, which makes it hard to capture the correlated joint
Unlike the aforementioned end-to-end models, by utiliz-
                                                                   distribution of speech rhythms. In this work, we suggest a
ing a conditional VAE, our model 1) learns to synthesize
                                                                   flow-based stochastic duration predictor that learns the joint
raw waveforms directly from text without requiring addi-
                                                                   distribution of the estimated phoneme duration, resulting in
tional input conditions, 2) uses a dynamic programming
                                                                   the generation of diverse speech rhythms in parallel.
method, MAS, to search the optimal alignment rather than
to calculate loss, 3) generates samples in parallel, and 4)
outperforms the best publicly available two-stage models.          6. Conclusion
                                                                   In this work, we proposed a parallel TTS system, VITS, that
5.2. Variational Autoencoders                                      can learn and generate in an end-to-end manner. We further
VAEs (Kingma & Welling, 2014) are one of the most widely           introduced the stochastic duration predictor to express di-
used likelihood-based deep generative models. We adopt a           verse rhythms of speech. The resulting system synthesizes
conditional VAE to a TTS system. A conditional VAE is a            natural sounding speech waveforms directly from text, with-
conditional generative model where the observed conditions         out having to go through predefined intermediate speech
modulate the prior distribution of latent variables used to        representations. Our experimental results show that our
generate outputs. In speech synthesis, Hsu et al. (2019) and       method outperforms two-stage TTS systems and achieves
Zhang et al. (2019) combine Tacotron 2 and VAEs to learn           close to human quality. We hope the proposed method will
speech style and prosody. BVAE-TTS (Lee et al., 2021) gen-         be used in many speech synthesis tasks, where two-stage
erates mel-spectrograms in parallel based on a bidirectional       TTS systems have been used, to achieve performance im-
VAE (Kingma et al., 2016). Unlike the previous works that          provement and enjoy the simplified training procedure. We
applied VAEs to first stage models, we adopt a VAE to a            also want to point out that even though our method inte-
parallel end-to-end TTS system.                                    grates two separated generative pipelines in TTS systems,
               Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

there remains a problem of text preprocessing. Investigating    Graves, A. Generating sequences with recurrent neural
self-supervised learning of language representations could        networks. arXiv preprint arXiv:1308.0850, 2013.
be a possible direction for removing the text preprocess-
ing step. We will release our source-code and pre-trained       Ho, J., Chen, X., Srinivas, A., Duan, Y., and Abbeel, P.
models to facilitate research in plenty of future directions.     Flow++: Improving flow-based generative models with
                                                                 variational dequantization and architecture design. In
                                                                 International Conference on Machine Learning, pp. 2722–
Acknowledgements                                                  2730. PMLR, 2019.
We would like to thank Sungwon Lyu, Bokyung Son,                Hsu, W.-N., Zhang, Y., Weiss, R., Zen, H., Wu, Y., Cao,
Sunghyo Chung, and Jonghoon Mo for helpful discussions            Y., and Wang, Y. Hierarchical Generative Modeling for
and advice.                                                       Controllable Speech Synthesis. In International Confer-
                                                                  ence on Learning Representations, 2019. URL https:
References                                                        //openreview.net/forum?id=rygkk305YQ.
Bernard, M. Phonemizer. https://github.com/                     Ito, K. The LJ Speech Dataset. https://keithito.
  bootphon/phonemizer, 2021.                                       com/LJ-Speech-Dataset/, 2017.

Bińkowski, M., Donahue, J., Dieleman, S., Clark, A., Elsen,    Jia, Y., Zhang, Y., Weiss, R. J., Wang, Q., Shen, J., Ren,
  E., Casagrande, N., Cobo, L. C., and Simonyan, K. High           F., Chen, Z., Nguyen, P., Pang, R., Lopez-Moreno, I.,
  Fidelity Speech Synthesis with Adversarial Networks. In          et al. Transfer Learning from Speaker Verification to
  International Conference on Learning Representations,            Multispeaker Text-To-Speech Synthesis. In Advances in
  2019.                                                            Neural Information Processing Systems, 2018.

Chen, J., Lu, C., Chenli, B., Zhu, J., and Tian, T. Vflow:      Kalchbrenner, N., Elsen, E., Simonyan, K., Noury, S.,
  More expressive generative flows with variational data          Casagrande, N., Lockhart, E., Stimberg, F., Oord, A.,
  augmentation. In International Conference on Machine            Dieleman, S., and Kavukcuoglu, K. Efficient neural au-
  Learning, pp. 1660–1669. PMLR, 2020.                            dio synthesis. In International Conference on Machine
                                                                  Learning, pp. 2410–2419. PMLR, 2018.
Chen, X., Kingma, D. P., Salimans, T., Duan, Y., Dhari-
  wal, P., Schulman, J., Sutskever, I., and Abbeel, P.          Kim, J., Kim, S., Kong, J., and Yoon, S. Glow-TTS: A Gen-
  Variational lossy autoencoder. 2017. URL https:                 erative Flow for Text-to-Speech via Monotonic Align-
  //openreview.net/forum?id=BysvGP5ee.                            ment Search. Advances in Neural Information Processing
                                                                  Systems, 33, 2020.
De Cheveigné, A. and Kawahara, H. Yin, a fundamental
                                                                Kingma, D. P. and Welling, M. Auto-Encoding Variational
  frequency estimator for speech and music. The Journal
                                                                  Bayes. In International Conference on Learning Repre-
  of the Acoustical Society of America, 111(4):1917–1930,
                                                                  sentations, 2014.
  2002.
                                                                Kingma, D. P., Salimans, T., Jozefowicz, R., Chen, X.,
Dinh, L., Sohl-Dickstein, J., and Bengio, S. Density esti-        Sutskever, I., and Welling, M. Improved variational in-
  mation using Real NVP. In International Conference on           ference with inverse autoregressive flow. Advances in
  Learning Representations, 2017.                                 Neural Information Processing Systems, 29:4743–4751,
Donahue, J., Dieleman, S., Binkowski, M., Elsen, E., and          2016.
  Simonyan, K. End-to-end Adversarial Text-to-Speech. In        Kong, J., Kim, J., and Bae, J. HiFi-GAN: Generative Ad-
 International Conference on Learning Representations,            versarial networks for Efficient and High Fidelity Speech
  2021. URL https://openreview.net/forum?                         Synthesis. Advances in Neural Information Processing
  id=rsf1z-JSj87.                                                 Systems, 33, 2020.
Durkan, C., Bekasov, A., Murray, I., and Papamakarios, G.       Kumar, K., Kumar, R., de Boissiere, T., Gestin, L., Teoh,
  Neural Spline Flows. In Advances in Neural Information         W. Z., Sotelo, J., de Brébisson, A., Bengio, Y., and
 Processing Systems, pp. 7509–7520, 2019.                         Courville, A. C. MelGAN: Generative Adversarial Net-
                                                                 works for Conditional waveform synthesis. volume 32,
Goodfellow, I., Pouget-Abadie, J., Mirza, M., Xu, B.,             pp. 14910–14921, 2019.
 Warde-Farley, D., Ozair, S., Courville, A., and Bengio,
 Y. Generative Adversarial Nets. Advances in Neural             Larsen, A. B. L., Sønderby, S. K., Larochelle, H., and
 Information Processing Systems, 27:2672–2680, 2014.              Winther, O. Autoencoding beyond pixels using a learned
               Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

  similarity metric. In International Conference on Ma-         Ren, Y., Ruan, Y., Tan, X., Qin, T., Zhao, S., Zhao, Z., and
  chine Learning, pp. 1558–1566. PMLR, 2016.                      Liu, T.-Y. FastSpeech: Fast, Robust and Controllable
                                                                  Text to Speech. volume 32, pp. 3171–3180, 2019.
Lee, Y., Shin, J., and Jung, K. Bidirectional Variational
  Inference for Non-Autoregressive Text-to-speech. In           Ren, Y., Hu, C., Tan, X., Qin, T., Zhao, S., Zhao, Z., and Liu,
  International Conference on Learning Representations,           T.-Y. FastSpeech 2: Fast and High-Quality End-to-End
  2021. URL https://openreview.net/forum?                         Text to Speech. In International Conference on Learning
  id=o3iritJHLfO.                                                 Representations, 2021. URL https://openreview.
                                                                  net/forum?id=piLPYqxtWuA.
Li, N., Liu, S., Liu, Y., Zhao, S., and Liu, M. Neural speech
   synthesis with transformer network. In Proceedings of the    Rezende, D. and Mohamed, S. Variational inference with
  AAAI Conference on Artificial Intelligence, volume 33,          normalizing flows. In International Conference on Ma-
   pp. 6706–6713, 2019.                                           chine Learning, pp. 1530–1538. PMLR, 2015.
Loshchilov, I. and Hutter, F. Decoupled Weight Decay            Shaw, P., Uszkoreit, J., and Vaswani, A. Self-Attention
  Regularization. In International Conference on Learning         with Relative Position Representations. In Proceedings
  Representations, 2019. URL https://openreview.                  of the 2018 Conference of the North American Chapter of
  net/forum?id=Bkg6RiCqY7.                                        the Association for Computational Linguistics: Human
                                                                  Language Technologies, Volume 2 (Short Papers), pp.
Ma, X., Zhou, C., Li, X., Neubig, G., and Hovy, E. Flowseq:
                                                                  464–468, 2018.
 Non-autoregressive conditional sequence generation with
 generative flow. In Proceedings of the 2019 Conference         Shen, J., Pang, R., Weiss, R. J., Schuster, M., Jaitly, N.,
 on Empirical Methods in Natural Language Processing              Yang, Z., Chen, Z., Zhang, Y., Wang, Y., Skerrv-Ryan, R.,
 and the 9th International Joint Conference on Natural            et al. Natural tts synthesis by conditioning wavenet on
 Language Processing (EMNLP-IJCNLP), pp. 4273–4283,               mel spectrogram predictions. In 2018 IEEE International
 2019.                                                            Conference on Acoustics, Speech and Signal Processing
                                                                  (ICASSP), pp. 4779–4783. IEEE, 2018.
Mao, X., Li, Q., Xie, H., Lau, R. Y., Wang, Z., and
 Paul Smolley, S. Least squares generative adversarial          Taigman, Y., Wolf, L., Polyak, A., and Nachmani, E.
 networks. In Proceedings of the IEEE international con-          Voiceloop: Voice Fitting and Synthesis via a Phono-
 ference on computer vision, pp. 2794–2802, 2017.                 logical Loop. In International Conference on Learning
                                                                  Representations, 2018. URL https://openreview.
Miao, C., Liang, S., Chen, M., Ma, J., Wang, S., and Xiao,
                                                                  net/forum?id=SkFAWax0-.
 J. Flow-TTS: A non-autoregressive network for text to
 speech based on flow. In ICASSP 2020-2020 IEEE In-             Valle, R., Shih, K. J., Prenger, R., and Catanzaro,
 ternational Conference on Acoustics, Speech and Signal           B. Flowtron: an Autoregressive Flow-based Gener-
 Processing (ICASSP), pp. 7209–7213. IEEE, 2020.                  ative Network for Text-to-Speech Synthesis. In In-
                                                                  ternational Conference on Learning Representations,
Oord, A. v. d., Dieleman, S., Zen, H., Simonyan, K.,
                                                                  2021. URL https://openreview.net/forum?
 Vinyals, O., Graves, A., Kalchbrenner, N., Senior, A.,
                                                                  id=Ig53hpHxS4.
  and Kavukcuoglu, K. Wavenet: A generative model for
  raw audio. arXiv preprint arXiv:1609.03499, 2016.             van den Oord, A., Vinyals, O., and Kavukcuoglu, K. Neural
                                                                  discrete representation learning. In Advances in Neural
Peng, K., Ping, W., Song, Z., and Zhao, K. Non-
                                                                  Information Processing Systems, pp. 6309–6318, 2017.
  autoregressive neural text-to-speech. In International
  Conference on Machine Learning, pp. 7586–7598.                Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones,
  PMLR, 2020.                                                     L., Gomez, A. N., Kaiser, Ł., and Polosukhin, I. Atten-
                                                                  tion is All you Need. Advances in Neural Information
Ping, W., Peng, K., Gibiansky, A., Arik, S. O., Kannan, A.,
                                                                  Processing Systems, 30:5998–6008, 2017.
  Narang, S., Raiman, J., and Miller, J. Deep Voice 3: 2000-
  Speaker Neural Text-to-Speech. In International Confer-       Veaux, C., Yamagishi, J., MacDonald, K., et al. CSTR
  ence on Learning Representations, 2018. URL https:              VCTK corpus: English multi-speaker corpus for CSTR
  //openreview.net/forum?id=HJtEm4p6Z.                            voice cloning toolkit. University of Edinburgh. The Cen-
                                                                  tre for Speech Technology Research (CSTR), 2017.
Prenger, R., Valle, R., and Catanzaro, B. Waveglow: A
  flow-based generative network for speech synthesis. In        Weiss, R. J., Skerry-Ryan, R., Battenberg, E., Mariooryad,
  ICASSP 2019-2019 IEEE International Conference on              S., and Kingma, D. P. Wave-Tacotron: Spectrogram-
  Acoustics, Speech and Signal Processing (ICASSP), pp.          free end-to-end text-to-speech synthesis. arXiv preprint
  3617–3621. IEEE, 2019.                                         arXiv:2011.03568, 2020.
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

Zeng, Z., Wang, J., Cheng, N., Xia, T., and Xiao, J. Aligntts:
  Efficient feed-forward text-to-speech system without ex-
  plicit alignment. In ICASSP 2020-2020 IEEE Interna-
  tional Conference on Acoustics, Speech and Signal Pro-
  cessing (ICASSP), pp. 6714–6718. IEEE, 2020.
Zhang, Y.-J., Pan, S., He, L., and Ling, Z.-H. Learning
  latent representations for style control and transfer in end-
  to-end speech synthesis. In ICASSP 2019-2019 IEEE
  International Conference on Acoustics, Speech and Sig-
  nal Processing (ICASSP), pp. 6945–6949. IEEE, 2019.
Ziegler, Z. and Rush, A. Latent normalizing flows for dis-
  crete sequences. In International Conference on Machine
  Learning, pp. 7673–7682. PMLR, 2019.
                            Supplementary Material of
        Conditional Variational Autoencoder with Adversarial Learning for
                            End-to-End Text-to-Speech


A. Monotonic Alignment Search
We present pseudocode for MAS in Figure 4. Although we search the alignment which maximizes the ELBO not the exact
log-likelihood of data, we can use the MAS implementation of Glow-TTS as described in Section 2.2.1.

           def monotonic_alignment_search(value):
               """Returns the most likely alignment for the given log-likelihood matrix.
               Args:
                   value: the log-likelihood matrix. Its (i, j)-th entry contains
                   the log-likelihood of the j-th latent variable
                   for the given i-th prior mean and variance:
                   .. math::
                       value_{i,j} = log N(f(z)_{j}; \mu_{i}, \sigma_{i})
                   (dtype=float, shape=[text_length, latent_variable_length])
               Returns:
                   path: the most likely alignment.
                   (dtype=float, shape=[text_length, latent_variable_length])
               """
               t_x, t_y = value.shape # [text_length, letent_variable_length]
               path = zeros([t_x, t_y])

                # A cache to store the log-likelihood for the most likely alignment so far.
                Q = -INFINITY * ones([t_x, t_y])

                for y in range(t_y):
                    for x in range(max(0, t_x + y - t_y), min(t_x, y + 1)):
                        if y == 0: # Base case. If y is 0, the possible x value is only 0.
                            Q[x, 0] = value[x, 0]
                        else:
                            if x == 0:
                                v_prev = -INFINITY
                            else:
                                v_prev = Q[x-1, y-1]
                            v_cur = Q[x, y-1]
                            Q[x, y] = value[x, y] + max(v_prev, v_cur)

                # Backtrack from last observation.
                index = t_x - 1
                for y in range(t_y - 1, -1, -1):
                    path[index, y] = 1
                    if index != 0 and (index == y or Q[index, y-1] < Q[index-1, y-1]):
                        index = index - 1

                return path

                                  Figure 4. Pseudocode for Monotonic Alignment Search.


B. Model Configurations
In this section, we mainly describe the newly added parts of VITS as we followed configurations of Glow-TTS and
HiFi-GAN for several parts of our model: we use the same transformer encoder and WaveNet residual blocks as those of
Glow-TTS; our decoder and the multi-period discriminator is the same as the generator and multi-period discriminator of
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

HiFi-GAN, respectively, except that we use different input dimension for the decoder and append a sub-discriminator.

B.1. Prior Encoder and Posterior Encoder
The normalizing flow in the prior encoder is a stack of four affine coupling layers, each coupling layer consisting of four
WaveNet residual blocks. As we restrict the affine coupling layers to be volume-preserving transformations, the coupling
layers do not produce scale parameters.
The posterior encoder, consisting of 16 WaveNet residual blocks, takes linear-scale log magnitude spectrograms and produce
latent variables with 192 channels.

B.2. Decoder and Discriminator
The input of our decoder is latent variables generated from the prior or posterior encoders, so the input channel size of the
decoder is 192. For the last convolutional layer of the decoder, we remove a bias parameter, as it causes unstable gradient
scales during mixed precision training.
For the discriminator, HiFi-GAN uses the multi-period discriminator containing five sub-discriminators with periods
[2, 3, 5, 7, 11] and the multi-scale discriminator containing three sub-discriminators. To improve training efficiency, we
leave only the first sub-discriminator of the multi-scale discriminator that operates on raw waveforms and discard two
sub-discriminators operating on average-pooled waveforms. The resultant discriminator can be seen as the multi-period
discriminator with periods [1, 2, 3, 5, 7, 11].




                                                                                         (c) Dilated and depth-wise separable convo-
          (a) Training procedure                      (b) Inference procedure
                                                                                         lutional residual block

Figure 5. Block diagram depicting (a) training procedure and (b) inference procedure of the stochastic duration predictor. The main
building block of the stochastic duration predictor is (c) the dilated and depth-wise separable convolutional residual block.


B.3. Stochastic Duration Predictor
Figures 5a and 5b show the training and inference procedures of the stochastic duration predictor, respectively. The main
building block of the stochastic duration predictor is the dilated and depth-wise separable convolutional (DDSConv) residual
block as in Figure 5c. Each convolutional layer in DDSConv blocks is followed by a layer normalization layer and GELU
activation function. We choose to use dilated and depth-wise separable convolutional layers for improving parameter
efficiency while maintaining large receptive field size.
The posterior encoder and normalizing flow module in the duration predictor are flow-based neural networks and have the
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

similar architecture. The difference is that the posterior encoder transforms a Gaussian noise sequence into two random
variables ν and u to express the approximate posterior distribution qφ (u, ν|d, ctext ), and the normalizing flow module
transforms d − u and ν into a Gaussian noise sequence to express the log-likelihood of the augmented and dequantized data
log pθ (d − u, ν|ctext ) as described in Section 2.2.2.
All input conditions are processed through condition encoders, each consisting of two 1x1 convolutional layers and a
DDSConv residual block. The posterior encoder and normalizing flow module have four coupling layers of neural spline
flows. Each coupling layer first processes input and input conditions through a DDSConv block and produces 29-channel
parameters that are used to construct 10 rational-quadratic functions. We set the hidden dimension of all coupling layers and
condition encoders to 192. Figure 6a and 6b show the architecture of a condition encoder and a coupling layer used in the
stochastic duration predictor.




    (a) Condition encoder in the stochastic duration predictor              (b) Coupling layer in the stochastic duration predictor

         Figure 6. The architecture of (a) condition encoder and (b) coupling layer used in the stochastic duration predictor.


C. Side-by-Side Evaluation
We conducted 7-point Comparative Mean Opinion Score (CMOS) evaluation between VITS and the ground truth through
500 ratings on 50 items. Our model achieved -0.106 and -0.270 CMOS on the LJ Speech and the VCTK datasets, respectively,
as in Table 5. It indicates that even though our model outperforms the best publicly available TTS system, Glow-TTS and
HiFi-GAN, and achieves a comparable score to ground truth in MOS evaluation, there remains a small preference of raters
towards the ground truth over our model.


                                  Table 5. Evaluated CMOS of VITS compared to the ground truth.
                                                        Dataset       CMOS
                                                        LJ Speech        -0.106
                                                        VCTK             -0.262


D. Voice Conversion
In the multi-speaker setting, we do not provide speaker identities into the text encoder, which makes the latent variables
estimated from the text encoder learn speaker-independent representations. Using the speaker-independent representations,
we can transform an audio recording of one speaker into a voice of another speaker. For a given speaker identity s and an
utterance of the speaker, we can attain a linear spectrogram xlin from the corresponding utterance audio. We can transform
xlin into a speaker-independent representation e through the posterior encoder and the normalizing flow in the prior encoder:

                                                          z ∼ qφ (z|xlin , s)                                                         (12)
                                                          e = fθ (z|s)                                                                (13)
                Conditional Variational Autoencoder with Adversarial Learning for End-to-End Text-to-Speech

Then, we can synthesize a voice ŷ of a target speaker identity ŝ from the representation e through the inverse transformation
of the normalizing flow fθ−1 and decoder G:

                                                         ŷ = G(fθ−1 (e|ŝ)|ŝ)                                                     (14)

Learning speaker-independent representations and using it for voice conversion can be seen as an extension of the voice
conversion method proposed in Glow-TTS. Our voice conversion method provides raw waveforms rather than mel-
spectrograms as in Glow-TTS. The voice conversion results are presented in Figure 7. It shows a similar trend of pitch
tracks with different pitch levels.




Figure 7. Pitch tracks of a ground truth sample and the corresponding voice conversion samples with different speaker identities.
